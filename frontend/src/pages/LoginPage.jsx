import AuthForm from "../components/AuthForm";
import "../styles/LoginPage.css";

const LoginPage = () => {
  return (
    <main>
      <AuthForm type="login" />
    </main>
  );
};

export default LoginPage;
